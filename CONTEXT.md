# Domain Context

- **Holding（持仓）**：一个股票代码及其买入记录、兼容字段和展示元数据。持仓由代码唯一识别。
- **Purchase Record（买入记录）**：一次买入的日期、单价和股数。多笔记录共同决定持仓股数、总成本和加权成本。
- **Portfolio（持仓组合）**：全部持仓及其排序、盈亏和人民币汇总结果。
- **Quote（行情）**：股票或指数的当前价、昨收、涨跌额、涨跌幅、名称和币种。
- **Market Snapshot（行情快照）**：一次刷新获得的持仓行情和三项指数行情。
- **Boss Key（老板键）**：复用兼容字段 `mofishMode` 持久化伪装状态；只改变列表呈现，不改变或删除持仓数据。

## Invariants

- `app_data` 是唯一持久化键，当前数据版本为 `1`。
- 存在非空 `records` 时，`shares` 和 `costPrice` 由记录汇总得到；否则兼容旧版 `shares/costPrice` 数据。
- 股票代码在访问行情时规范化，但现有持仓代码只有在行情纠错成功后才回写。
- 港币和美元盈亏汇总继续使用固定换算值 `0.92` 和 `7.2`。
