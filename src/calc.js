const HKD_TO_CNY_FALLBACK=.92,USD_TO_CNY_FALLBACK=7.2;function calcHoldingStats(holding,quote){let records=holding.records;records&&Array.isArray(records)&&0!==records.length||(records=holding.shares&&null!=holding.costPrice?[{date:holding.buyDate||"",price:Number(holding.costPrice),shares:Number(holding.shares)}]:[]);let totalShares=0,totalCost=0;records.forEach(r=>{const s=Number(r.shares)||0,p=Number(r.price)||0;s>0&&(totalShares+=s,totalCost+=s*p)});const avgCost=totalShares>0?totalCost/totalShares:null!=holding.costPrice?Number(holding.costPrice):null,shares=totalShares>0?totalShares:Number(holding.shares)||0;let holdingProfitVal=null,holdingProfitRateVal=null;return quote&&null!=avgCost&&shares>0&&(holdingProfitVal=shares*(quote.current-avgCost),avgCost>0&&(holdingProfitRateVal=(quote.current-avgCost)/avgCost*100)),{totalShares:shares,totalCost:totalCost,avgCost:avgCost,holdingProfit:holdingProfitVal,holdingProfitRate:holdingProfitRateVal}}function todayProfit(holding,quote){if(!quote)return 0;return calcHoldingStats(holding,quote).totalShares*(quote.current-quote.prevClose)}function holdingProfit(holding,quote){if(!quote)return null;return calcHoldingStats(holding,quote).holdingProfit}function holdingProfitRate(holding,quote){if(!quote)return null;return calcHoldingStats(holding,quote).holdingProfitRate}function toCNY(amount,currency,hkdFx=.92,usdFx=7.2){return"HKD"===currency?amount*hkdFx:"USD"===currency?amount*usdFx:amount}function totals(holdings,quotes){let totalTodayProfit=0,totalHoldingProfit=0;return holdings.forEach(h=>{const q=quotes[h.code]||("function"==typeof normalizeCode?quotes[normalizeCode(h.code)]:null);if(!q)return;totalTodayProfit+=toCNY(todayProfit(h,q),q.currency);const hp=holdingProfit(h,q);null!=hp&&(totalHoldingProfit+=toCNY(hp,q.currency))}),{totalTodayProfit:totalTodayProfit,totalHoldingProfit:totalHoldingProfit}}